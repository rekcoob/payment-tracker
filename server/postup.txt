npm init
npx tsc --init

// dependencies
yarn add express dotenv mongoose colors morgan

// dev dependencies
yarn add -D nodemon concurrently 

// TS dev dependencies
yarn add -D typescript @types/node @types/express @types/mongoose
yarn add -D @types/morgan


1.07 build

TransactionItem {transaction}
Number with spaces

fix morgan dev

Proxy and long localhost strings or env.local
Start dev server with concurrently
